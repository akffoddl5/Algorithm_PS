#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <sstream>
#include <queue>

using namespace std;

int main() {
	int tc;
	cin >> tc;

	for (int i = 0; i < tc; i++) {
		string input;
		int input_num;
		string input_list;
		cin >> input;
		cin >> input_num;
		cin >> input_list;

		queue<int> a;
		a.front();

		
		
		
		vector<int> r;
		input_list = input_list.substr(1, input_list.length() - 1 - 1);
		istringstream stream(input);
		string buffer;

		while (getline(stream, buffer, ',')) {

		}
		
		//input_list.




	}


}